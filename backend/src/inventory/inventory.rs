use rust_decimal_macros::dec;
use uuid::Uuid;

use super::product;

/* 
pub fn get_product(id: Uuid) {}
 
pub fn get_all_products() -> Option<Vec<product::Product>> {
    let mut result: Vec<product::Product> = Vec::new();
    let product_1 = product::Product {
        id: Uuid::new_v4(),
        name: "dummyproduct".to_string(),
        price: dec!(1.99),
    };
    result.push(product_1);
    Some(result)
}
*/